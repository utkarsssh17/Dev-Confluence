<div class="custom-card">
    <img class="custom-card-image" src={{imageUrl}} alt={{title}}>
    <div class="custom-card-body">
    <div class="custom-card-left-block">
        <div class="custom-card-title">{{title}}</div>
        <div class="custom-card-location">
        <i class="fa-solid fa-location-dot location-icon" style="color: #f76e40;"></i>
        {{city}}
        </div>
    </div>
    <div class="custom-card-right-block">
        <div class="custom-card-date">{{day}}</div>
        <div class="custom-card-time">{{time}}</div>
    </div>
    </div>
</div>